server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: file:///github.com/rituku/config-server.git
          clone-on-start: true
          skip-ssl-validation: true